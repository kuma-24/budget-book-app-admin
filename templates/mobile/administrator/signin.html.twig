{% extends 'mobile/_base_layout/base.html.twig' %}

{% block title %}
    サインイン
{% endblock %}

{% block importmap %}
    {{ importmap(['mobile-base-layout', 'mobile-administrator-signin']) }}
{% endblock %}

{% block body %}
    {% if error %}
        <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}
    
    {{ form_start(form) }}
        {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}